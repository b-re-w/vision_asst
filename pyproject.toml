[project]
name = "vision-asst"
version = "0.1.0"
description = "Vision Assistant for Visually-impaired People"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.11.0",
    "transformers>=5.8.0",
    "vllm>=0.20.1",
    "vllm-omni[demo]>=0.20.0",
]
